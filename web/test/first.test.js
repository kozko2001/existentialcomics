import {expect} from 'chai'

describe('actions', () => {
  describe('actions', () => {
    it('should create an action to add a todo', () => {
      var x = 2;
      expect(x).to.be.equal(2)
    })
  })
})

